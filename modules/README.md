# IPNIS Sample Modules

This directory shows some module categories, such as [ `vision` ](vision).

You can find some sample modules as you want.

```bash
cargo run --release --package ipnis-modules-{module}-example
```
